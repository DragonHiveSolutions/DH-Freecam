-- config.lua
Config = {}

-- Define the range within which the camera can move
Config.CameraRange = 100.0 -- Adjust this value to set the maximum distance the camera can move

-- Define the key to activate free cam (optional)
Config.ActivationCommand = "freecam"
